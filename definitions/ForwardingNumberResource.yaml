{
  "type": "object",
  "properties": {
    "uri": {
      "type": "string",
      "format": "uri"
    },
    "id": {
      "type": "string"
    },
    "phoneNumber": {
      "type": "string"
    },
    "label": {
      "type": "string"
    },
    "features": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "CallFlip",
          "CallForwarding"
        ]
      }
    },
    "flipNumber": {
      "type": "string",
      "pattern": "[1-8]"
    },
    "type": {
      "type": "string",
      "description": "Forwarding phone number type",
      "enum": [
        "Home",
        "Mobile",
        "Work",
        "PhoneLine",
        "Outage",
        "Other"
      ]
    }
  }
}